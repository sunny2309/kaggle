if __name__ == '__main__':
    N = int(raw_input())
    for i in range(N):
        t1 = raw_input()
        t2 = raw_input()
        decide = 'BOTH' if '!' in t1 and '!' in t2 else 'T1' if '!' in t1 else 'T2' if '!' in t2 else 'NONE'
        if decide == 'BOTH':
            first_ex = len(t1.split('!')[1])+1
            second_ex = len(t2.split('!')[1])+1
            first = int(t1.split('!')[0])
            second = int(t2.split('!')[0])
            'EQ' if first_ex == second_ex else 'FIRST' if first_ex > second_ex else 'SECOND'
           
        elif decide == 'T1':
            pass
            n = int(t1.split('!')[0])
            j = int(t2)
            print()
        elif decide == 'T2':
            pass
        else:
            print(1 if int(t1) > int(t2) else -1 if int(t1) < int(t) else 0)
        
